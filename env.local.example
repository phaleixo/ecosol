# Exemplo de arquivo .env.local (não conter valores reais)
# Copie este arquivo para `.env.local` e preencha os valores sensíveis.

# Opcional: token usado pela Vercel (geralmente gerenciado pela Vercel)
VERCEL_OIDC_TOKEN=

# -----------------------------------------------------------------------------
# BANCO DE DADOS (PRISMA + SUPABASE)
# -----------------------------------------------------------------------------
# Conexão via Pooler (uso em runtime/serverless)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?pgbouncer=true"

# Conexão direta (migrations/introspection)
DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Necessário para runtime do Prisma na Vercel
PRISMA_CLI_QUERY_ENGINE_TYPE=binary

# -----------------------------------------------------------------------------
# SUPABASE AUTH & STORAGE (PUBLIC)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# -----------------------------------------------------------------------------
# SERVIÇOS DE E-MAIL
# -----------------------------------------------------------------------------
# Gmail (Nodemailer)
GMAIL_USER=
GMAIL_PASS=

# Resend (API Key)
RESEND_API_KEY=

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE AMBIENTE
# -----------------------------------------------------------------------------
# URL pública do site (usada em e-mails, redirecionamentos)
NEXT_PUBLIC_SITE_URL=

# NextAuth / sessão
NEXTAUTH_SECRET=
NEXTAUTH_URL=

# Integrações OAuth (opcional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Observação: mantenha este arquivo fora do repositório com valores reais.
# Use `.env.local.example` apenas como referência de chaves necessárias.
